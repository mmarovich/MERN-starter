const mongoose = require("mongoose");
const Schema = mongoose.Schema;

// const specificSchema = new Schema({
//   something: { type: String, required: true },
//   somethingElse: { type: String, required: true },
// });

// const Specific = mongoose.model("Specific", specificSchema);

// module.exports = Specific;
